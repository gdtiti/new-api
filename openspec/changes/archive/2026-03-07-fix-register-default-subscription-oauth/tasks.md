## 1. 配置清空支持

- [x] 1.1 调整 `SystemSetting.jsx` 的默认套餐保存逻辑，允许管理员提交空值以清空配置
- [x] 1.2 调整默认套餐配置校验，允许“关闭功能 + 空套餐”，但继续拒绝“启用功能 + 空套餐”

## 2. GitHub 注册默认套餐一致性

- [x] 2.1 核对并修正 GitHub OAuth 新用户注册后的默认套餐发放链路
- [x] 2.2 补充 GitHub OAuth 新注册默认套餐发放的回归测试

## 3. 验证

- [x] 3.1 运行注册默认套餐相关的定向测试，确认普通注册与 GitHub OAuth 注册行为一致
