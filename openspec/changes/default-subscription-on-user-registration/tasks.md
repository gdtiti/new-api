## 1. 配置中心与管理端配置

- [x] 1.1 在配置中心增加“注册默认订阅开关”和“默认订阅套餐标识”字段，并补充读写与运行态同步逻辑
- [x] 1.2 在后台配置保存流程中增加默认套餐有效性校验，拒绝不存在或已停用的套餐配置
- [x] 1.3 在管理端配置界面暴露默认注册订阅配置项，并支持启用、关闭和选择目标套餐

## 2. 订阅领域能力扩展

- [x] 2.1 为订阅领域增加“系统发放默认注册订阅”的服务或模型方法，明确与支付订单完结链路分离
- [x] 2.2 为默认发放增加来源标记或等效幂等信息，确保同一用户最多获得一次注册默认订阅
- [x] 2.3 校验发放后的用户订阅记录可被现有订阅查询接口正确返回

## 3. 注册流程接入

- [x] 3.1 在受支持的用户注册成功路径中接入默认订阅发放逻辑，并仅在用户创建成功后执行
- [x] 3.2 处理默认套餐失效和内部发放错误场景，确保注册成功结果不被回滚
- [x] 3.3 为注册默认订阅发放补充结构化日志或可观测信息，便于排查配置失效与发放失败

## 4. 验证与回归

- [x] 4.1 为配置校验、默认发放成功、重复触发幂等、发放失败不阻断注册补充后端测试
- [x] 4.2 验证普通注册与受支持的第三方注册路径下默认订阅行为一致
- [x] 4.3 验证新用户注册后可在订阅查询或前端订阅视图中看到默认发放的套餐权益
